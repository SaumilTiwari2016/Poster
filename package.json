{
  "name": "mern-socialmedia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js && node socket/index.js && npm --prefix client start",
    "server": "node server/index.js",
    "socket": "node socket/index.js",
    "client": "npm --prefix client start",
    "build": "cd server && npm i && cd .. && cd socket && npm i && cd .. && cd client && npm i && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
