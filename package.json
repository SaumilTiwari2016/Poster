{
  "name": "mern-socialmedia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && node index.js  && cd .. && cd socket && node index.js  && cd .. && cd client && npm start",
    "build": "cd server && npm i && cd .. && cd socket && npm i && cd .. && cd client && npm i && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
